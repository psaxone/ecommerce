export enum ROUTES {
  HOME = '/',
  DETAILS = '/details',
  CART = '/cart',
  CHECKOUT = '/checkout',
}

export enum ROUTESNAMES {
  HOME = 'Home',
  DETAILS = 'Details',
  CART = 'Cart',
  CHECKOUT = 'Checkout',
}